<%= form_with(model: chlo) do |form| %>
  <% if chlo.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(chlo.errors.count, "error") %> prohibited this chlo from being saved:</h2>

      <ul>
        <% chlo.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
